[tool.poetry]
authors = ["wagner.castro <wagner.castro@m2center.com>"]
description = ""
name = "api-de-test"
packages = [
  {include = "src", from = "."},
]
readme = "README.md"
version = "1.0.0"

[tool.poetry.dependencies]
flask = "^3.1.0"
flask-migrate = "^4.1.0"
flask-sqlalchemy = "^3.1.1"
python = "^3.11"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]
